<h1>Listing Monthly Tasks</h1>

<%= form_for(Month.new, remote:true) do |f| %>
  <%= f.text_field :title, placeholder: "New Task", id:"new-month", autofocus:true, "data-behavior" => "submit_on_enter" %>
<% end %>

<div id="task_list">
  <%= render @months.to_a %>
</div>
